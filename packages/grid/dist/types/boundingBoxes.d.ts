import { BoundingBox } from './types';
/**
 * Check if the provided bounding boxes touch/overlap.
 * We assume that the boxes are rectangular and axis-aligned.
 */
export declare const areBoundingBoxTouching: (boxA: BoundingBox, boxB: BoundingBox) => boolean;
//# sourceMappingURL=boundingBoxes.d.ts.map